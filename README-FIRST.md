# First step

1. Extract this package into `C:\Users\theon\GitHub\TrackPrompt-Studio`.
2. Double-click `RUN-ANDROMEDA-FORENSIC-AUDIT.cmd`.
3. Upload the generated `andromeda-forensic-upload.zip` from the newest `test-output\andromeda-forensic-*` directory.

The audit is read-only and will tell us exactly whether the remaining blocker is a missing human record, stale held release identity, aggregate forecast, missing artifact binding, or private source path.
