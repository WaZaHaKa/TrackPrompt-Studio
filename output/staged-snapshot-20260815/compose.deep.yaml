# Generated by setup-deep-gpu-docker.ps1
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.deep
    gpus: all
    environment:
      ENABLE_DEMUCS: "true"
      DEMUCS_MODEL_NAME: "htdemucs"
      DEMUCS_DEVICE: "cuda"
      MODEL_CACHE_DIR: "/data/models"
